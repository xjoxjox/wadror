<p id="notice"><%= notice %></p>

<h2>
  <strong></strong>
  <%= @brewery.name %>
</h2>

<p>
  <i>Established in <%= @brewery.year %></i>

</p>

<p> Number of beers: <%= @brewery.beers.count %> </p>

<ul>
  <% @brewery.beers.each do |beer| %>
      <li> <%= link_to beer.name, beer %> </li>
  <% end %>
</ul>

<p>
  <%= link_to 'List of beers', beers_path %>
</p>

<% if @brewery.ratings.empty? %>
    <p>Brewerys beers has not yet been rated!</p>
<% else %>
    <p>Brewerys beers has <%= pluralize(@brewery.ratings.count, 'rating') %>,
      average <%= @brewery.average_rating(@brewery.beers) %></p>
<% end %>

<%= link_to 'Edit', edit_brewery_path(@brewery) %> |
