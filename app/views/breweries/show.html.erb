<h2>
  <strong></strong>
  <%= @brewery.name %>
  <% if not @brewery.active  %>
      <span class="label label-info">retired</span>
  <% end %>
</h2>

<p>
  <i>Established at <%= @brewery.year %></i>

</p>

<p> Number of beers: <%= @brewery.beers.count %> </p>

<ul class="list-unstyled">
  <% @brewery.beers.each do |beer| %>
      <li> <%= link_to beer.name, beer %> </li>
  <% end %>
</ul>

<p>
  <%= link_to 'List of beers', beers_path %>
</p>

<p>Beers have <%= pluralize(@brewery.ratings.count, 'rating') %> average <%= @brewery.average_rating %></p>

<%= link_to 'Edit', edit_brewery_path, class:"btn btn-primary" if current_user %>
<%= link_to 'Destroy', @brewery, method: :delete, data:
    { confirm: 'Are you sure?' }, class:"btn btn-danger" if current_user %>
